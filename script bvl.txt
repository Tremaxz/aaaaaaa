local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()

local Window = Rayfield:CreateWindow({
   Name = "🔥 Sushi HUB 🔫",
   LoadingTitle = "🔫 Beyond Volleyball League 💥",
   LoadingSubtitle = "by #Sushijimas",
   ConfigurationSaving = {
      Enabled = false,
      FolderName = nil, -- Create a custom folder for your hub/game
      FileName = "Example Hub"
   },
   Discord = {
      Enabled = false,
      Invite = "noinvitelink", -- The Discord invite code, do not include discord.gg/. E.g. discord.gg/ABCD would be ABCD
      RememberJoins = true -- Set this to false to make them join the discord every time they load it up
   },
})

-- Criação da aba "Home"
local MainTab = Window:CreateTab("🏠 Home", nil) -- Título, imagem

local Input = MainTab:CreateInput({
   Name = "JumpHeight",
   PlaceholderText = "1-500",
   RemoveTextAfterFocusLost = true,
   Callback = function(Value)
              local player = game.Players.LocalPlayer
      local character = player.Character or player.CharacterAdded:Wait()

      -- Usando exatamente o código fornecido para alterar o JumpHeigh

      game.Workspace[player.Name].Humanoid.JumpHeight = Value
      game.Workspace[player.Name].Humanoid.AutoJumpEnabled = true
      game.Workspace[player.Name].Humanoid.AutomaticScalingEnabled = false

      game.Workspace[player.Name].Humanoid.UseJumpPower = false

	end
})

local Input = MainTab:CreateInput({
   Name = "Tilt",
   PlaceholderText = "200-500",
   RemoveTextAfterFocusLost = true,
   Callback = function(Value)
              local player = game.Players.LocalPlayer
      local character = player.Character or player.CharacterAdded:Wait()

      -- Usando o RenderStepped para aplicar continuamente o valor
      game:GetService("RunService").RenderStepped:Connect(function()
         game.Workspace[player.Name].HumanoidRootPart.Tilt.D = (Value)
      end)
   end,
})